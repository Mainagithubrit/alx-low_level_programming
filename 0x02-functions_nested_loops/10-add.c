#include "main.h"


/**
 * add - a function that adds two integers and returns the result
 * @x: The first number
 * @y: The second number
 * Return: The result
 */
int add(int x, int y)
{
int c;
c = x + y;
return (c);
}
